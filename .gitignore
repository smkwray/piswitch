.build/
dist/
run/
.DS_Store
*.swp

# Windows build artifacts
windows/PiSwitch/bin/
windows/PiSwitch/obj/

# Stale scripts directory (superseded by scripts/*-windows.ps1)
scripts-windows/

# Local/private configs (use config/examples for public samples)
config/instances/*.json
!config/instances/.gitkeep

# Local/private Finder group bundles
assets/finder-groups/

# Keep only the public README in version control
*.md
!/README.md
